# Quality ladder (Phase 5) — after press-Play works

Status vocabulary: honest tags only.

| Step | Status | Notes |
|------|--------|-------|
| Simulation Chamber flipbook press-Play | **partial_with_gaps** | Done for warrior + book-drop heuristic |
| Production sculpt (replace fixture fox) | **declared** | Sovereign Sculptor + `rig/*` / `region.*` / hash-before-load (`feat/rt4d-preview-contracts`) |
| Engine3D soft-raster camera paths | **declared** / **partial** elsewhere | Book-movie soft-raster exists under tmp/showcase paths — wire as chamber backend later |
| Daniel blueprint Cycles | **partial** adapter shell | Optional higher-fidelity stills; CUDA-leaning — not RX 580 demo path |
| Cosmos Transfer | **declared optional** | Rented NVIDIA polish only; `cosmosRequired: false` forever for local chamber |
| Identity / preview contracts | **partial** | Consume `warrior-anthro-fox-01` naming; do not fork |

## Operator rule

Do not climb this ladder until `demo-short-warrior.sh` and `book_drop.py` produce a watchable `press-play.mp4` with identity locks green.
