# Capability: `neural_cinematic_simulation_backend`

Mandala-side adapter for Infinity Visual Lane–shaped requests.
**Not** Story Forge itself. **Not** Movie Lane.

| Field | Value |
|-------|--------|
| id | `neural_cinematic_simulation_backend` |
| schemaVersion | `neural-cinematic/0.1` |
| host organ | Mandala (Visual Body) |
| motion organ | Mandala Simulation Chamber |
| cosmos | optional / skipped for local |

Enforcement: narrative identity only via explicit `characterId` / `identityLock`
compatible with `storyforge-mandala-contract/1.1`. Filenames are never identity.
