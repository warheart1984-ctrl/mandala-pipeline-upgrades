# Declared GPU sketches

These GLSL files copy the locked user source. They are **declared** — not dispatched on Polar, not SPIR-V, not enforced.

`HoloRT4D_BinPaths.comp.glsl` and `Debug_*.glsl` are the A/B + debug suite as written.
