// Material system stub with evidence fields
export class Material { constructor(name, params={}){ this.name=name; this.params=params; this.intentId=null; this.worldId=null; this.timelineId=null; this.timeSeconds=0; } }
