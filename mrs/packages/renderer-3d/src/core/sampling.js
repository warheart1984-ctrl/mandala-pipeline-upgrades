// Sampling utilities
export function pcgHash(input){ let state = input * 747796405 + 2891336453; /* stub */ return state; }
export function cosineWeightedSample(){ return {x:0,y:0,z:1}; }
// Provenance preserved
