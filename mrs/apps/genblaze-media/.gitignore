.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/
data/*.json
# Ephemeral local preview stills (served via /api/preview/{run_id}); never commit
data/preview-cache/
# Operator photo ingest store (POST /api/image/ingest); never commit uploads
data/ingested/
.env
.env.*
!.env.example
# Local scratch dumps only (do not ignore app/__init__.py)
_scratch_*.py
_scratch_*.json
_scratch_*.txt
