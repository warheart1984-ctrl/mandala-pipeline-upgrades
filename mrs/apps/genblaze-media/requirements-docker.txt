# Production deps only (pytest stays in requirements.txt for local/CI)
# Pillow is NOT listed here: genblaze-core==0.3.8 still declares pillow<12, which conflicts
# with the CVE pin Pillow==12.3.0 under modern pip. Dockerfiles install packages from
# this file, then overlay with: pip install --no-deps Pillow==12.3.0
#
# SoT for Render/hackathon App URL: repo-root Dockerfile (context = repo root).
# App-local Dockerfile is NVIDIA-only (no RT4D). Both install this file, then
# optionally install requirements-gmi.txt (genblaze-gmicloud) with non-fatal fallback.
# GMI_API_KEY is never baked into the image — set on Render dashboard at runtime.
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv==1.2.2
pydantic==2.11.7
httpx==0.28.1
# Required for POST /api/image/ingest multipart (Starlette request.form).
# Keep in sync with requirements.txt. Both Dockerfiles install this file.
python-multipart==0.0.31
genblaze-core==0.3.8
genblaze-s3==0.3.6
genblaze-nvidia==0.3.3
boto3==1.39.14
# Blank/near-black still detection (app/image_quality.py) — must match requirements.txt pin
Pillow==12.3.0
