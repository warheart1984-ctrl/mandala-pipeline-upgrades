# Pinned for Backblaze Genblaze hackathon MVP (Python >= 3.11)
# Install: pip install --use-deprecated=legacy-resolver -r requirements.txt
# (genblaze-core 0.3.8 still declares pillow<12; Pillow 12.3.0 is kept for CVE fixes)
fastapi==0.116.1
uvicorn[standard]==0.35.0
python-dotenv==1.2.2
pydantic==2.11.7
httpx==0.28.1
# Required for POST /api/image/ingest multipart (Starlette request.form).
# Pin must match requirements-docker.txt (used by root + app Dockerfiles).
python-multipart==0.0.31

# Genblaze stack (NVIDIA NIM image + B2/S3 storage)
# genblaze-nvidia 0.3.3 currently targets genblaze-core >=0.3.7,<0.4
genblaze-core==0.3.8
genblaze-s3==0.3.6
genblaze-nvidia==0.3.3

# Explicit boto3 for health/list helpers (also pulled by genblaze-s3)
boto3==1.39.14
# Blank/near-black still detection after NIM decode
Pillow==12.3.0
pytest==9.1.1
