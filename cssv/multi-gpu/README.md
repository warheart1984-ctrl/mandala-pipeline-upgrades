# Multi-GPU CSSV evidence directory

> Phase C **declared**. Schema: [`cssv-multi-gpu-schema.json`](./cssv-multi-gpu-schema.json).  
> Runtime appends `frames.ndjson` (not committed — comments would be invalid NDJSON).
