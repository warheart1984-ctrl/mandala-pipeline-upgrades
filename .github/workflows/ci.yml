name: Mandala Rendering CI

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  lint:
    # Blocking ESLint gate over first-party JS/ESM. `npm run lint` (eslint .)
    # exits non-zero on ERROR-level problems only (warnings do not fail the
    # build), so this job fails the pipeline exactly on genuine lint errors.
    # NOTE: this gate depends on branches #38 (eslint config) and #33
    # (RT4DGPURenderer.js parse-error fix). Until #33 lands, that single
    # remaining parse error keeps this job red; every other error is resolved.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
      - run: npm test

  governance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run governance tests
        run: npm run test:governance

  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Run conformance tests
        run: npm run test:conformance

  runtime-provenance-replay:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Provenance + Replay unit tests
        run: npm run test:runtime-provenance

  surface-sync:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Check surface mesh sync
        run: npm run sync:surfaces -- --dry-run

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Package type consistency
        run: node scripts/check-package-types.mjs

  engine3d:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Install engine3d dependencies
        run: npm ci || npm install
        working-directory: mrs/packages/engine3d-core
      - name: Build TypeScript then run engine3d tests
        # package script: npm run build && node --test dist/test/...
        run: npm run test:engine3d
        working-directory: mrs/packages/engine3d-core

  infinity-director:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install Infinity Director deps
        run: pip install -e ".[dev]"
        working-directory: mrs/apps/infinity-director
      - name: Pytest (IDAC route + charter gates; live tests skip unless env set)
        run: python -m pytest -q
        working-directory: mrs/apps/infinity-director

  # Genblaze hackathon demo-cache — unit + schedule hint only (no API keys).
  genblaze-demo-cache:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install Genblaze deps (no GMI key / no live SDK required)
        working-directory: mrs/apps/genblaze-media
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Unit tests + --schedule-hint-only
        working-directory: mrs/apps/genblaze-media
        env:
          GENBLAZE_DRY_RUN: "1"
          GENBLAZE_SKIP_LOCAL_SD: "1"
        run: |
          python -m pytest tests/test_demo_cache.py -q
          python -m app.pre_render --plan plans/demo-shot-plan.example.json --schedule-hint-only --skip-local-sd

  release-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Version alignment (release.json ↔ packages ↔ charter)
        run: npm run release:check

  mandala-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      # 4d-renderer/ is a re-export shim (no junction required for package imports).
      # Optional symlink only if a heuristic tool walks deep 4d-renderer/src paths.
      - name: Setup optional 4d-renderer/src symlink
        run: ln -sfn "$PWD/mrs/packages/renderer-core/src" "$PWD/4d-renderer/src"
      - name: Constitutional lint
        run: node mandala-agent-pack/lint/run-lint.js
      - name: Generate drift radar
        run: node mandala-agent-pack/drift-radar/generate-report.js
      - name: GPU mock + constitution (skip-ok live separately)
        run: npm run test:gpu
      - name: MultiHost constitutional routing
        run: npm run test:multihost
      - name: Live WebGPU (skip-ok without adapter)
        run: npm run test:gpu-live
      - name: Upload drift radar
        uses: actions/upload-artifact@v4
        with:
          name: drift-radar
          path: drift-radar.json

  webgpu-live-optional:
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
      - name: Optional WebGPU live (assist paths only; print SoT = cpu.rt4d.print)
        run: npm run test:gpu-live
