# capabilities slot

Capability metadata lives in `sovereign-x/router/registry/gpuSkillsRegistry.json`.

Namespace: `sx.capability.gpu.*` / `sx.capability.cpu.rt4d.print`

## Declared / prototype (non-authoritative)

| Capability | Status | Authority |
|------------|--------|-----------|
| `gpu.integrator.deterministic` | **declared** prototype | assist only — never print SoT |

Seed contract (declared): mulberry32 + stratified sampling — see
`docs/governance/cecp/trails/gpu-determinism-2026-09/`.
