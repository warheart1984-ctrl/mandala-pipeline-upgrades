{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "RC.Mandala.v1",
  "title": "RC.Mandala.v1 — Mandala Render Replay Contract",
  "description": "Declared reconstruction rules stub. Not executed by Continuity Ledger v1.",
  "type": "object",
  "required": ["rc_id", "version", "inputs", "reconstruction"],
  "properties": {
    "rc_id": { "const": "RC.Mandala.v1" },
    "version": { "const": "1" },
    "inputs": {
      "type": "array",
      "items": { "enum": ["CES.Mandala.Render.v1", "CES.Lineage.Identity.v1"] }
    },
    "reconstruction": {
      "type": "string",
      "description": "Declared: rebuild render provenance from intent/world/timeline + asset_hash."
    },
    "determinism": { "const": "required" }
  }
}
