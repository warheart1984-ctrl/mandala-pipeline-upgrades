{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "RC.Lineage.v1",
  "title": "RC.Lineage.v1 — Lineage Identity Replay Contract",
  "description": "Declared reconstruction rules stub. Not executed by Continuity Ledger v1.",
  "type": "object",
  "required": ["rc_id", "version", "inputs", "reconstruction"],
  "properties": {
    "rc_id": { "const": "RC.Lineage.v1" },
    "version": { "const": "1" },
    "inputs": {
      "type": "array",
      "items": { "enum": ["CES.Lineage.Identity.v1"] }
    },
    "reconstruction": {
      "type": "string",
      "description": "Declared: walk parent_identity_id chain to reconstruct identity provenance."
    },
    "determinism": { "const": "required" }
  }
}
