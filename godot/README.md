# Godot host

Godot 4 simulation chamber lives in `godot/GovernedChamberProject/`.
Open that folder as the Godot project root in Godot 4.3+.

**Status: skeleton** — Phase 1 host scaffold (blocked scene + director beats +
JSON recorder). Open and verify in Godot 4.3+ before claiming end-to-end runs.

Phase 1 milestone: blocked scene with AI dialogue — see
`GovernedChamberProject/README.md` for run instructions and module map.

Provenance: ported from sibling worktree
`mrs-feat-neural-cinematic-engine/godot/GovernedChamberProject/` into this repo
to match the `unity/` / `unreal/` top-level host convention.
