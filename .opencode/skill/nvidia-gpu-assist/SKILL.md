---
name: nvidia-gpu-assist
description: NVIDIA GPU assist skills for Sovereign X (FLUX ingest, host checks). Assist-only — never Digital Printer SoT.
---
# nvidia-gpu-assist

- `flux_generate.js` — NIM FLUX shell image ingest (`lookdev-from-image`)
- Always `assistOnly=true`; print SoT remains `cpu.rt4d.print`
