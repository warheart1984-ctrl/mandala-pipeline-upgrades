# Full-stack showcase (partial)

One command exercises as many Mandala organs as fit on ~15GB RAM / FX-8350 / RX 580, then writes a labeled collage.

```bash
node scripts/full-stack-showcase.mjs
```

Open `output/mandala-full-stack/final.png`. Optional: `final.mp4`, `receipt.json` (ran | skipped | error per subsystem). Not a production film.
