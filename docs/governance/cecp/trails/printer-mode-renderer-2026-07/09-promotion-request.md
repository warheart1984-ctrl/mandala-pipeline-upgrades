# 09 — Promotion request

**Trail:** `printer-mode-renderer-2026-07`  
**Request:** **DENIED / not filed** — ESFR `HOLD` (`08-esfr-verdict.json`)

## Requested eligibility

~~`PROMOTE_WITHOUT_GAPS` / `PROMOTE`~~ — **not requested**.

## Current eligibility

**`HOLD`** (user-facing equivalent: **HOLD_WITH_GAPS**)

## Required before re-request

1. Soft penumbra deterministic + tested → **enforced**  
2. RT4D specular print plates / fixtures → **enforced** (beyond library existence)  
3. Denoise default policy settled + multi-backend coverage if claimed universal  
4. Unity / Unreal / Engine mesh SHA sync verified **or** explicitly scoped out  
5. Live CSR / GovernanceDecision emission **or** downgrade those checklist rows permanently  
6. Optional: re-run governance suite and cite evidence for A-row checks  

## What may ship under HOLD

- Printer adapter + demo path (prior `PROMOTE_WITH_GAPS` core)  
- `/printer` HTTP surface (opt-in execute)  
- Quality profiles + print timeout env  
- Opt-in CPU bilateral denoise on `render-scene`  

Promotion re-request file to be opened only after E-list closes with tests.
