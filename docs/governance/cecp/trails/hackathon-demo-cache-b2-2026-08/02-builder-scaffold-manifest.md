# 02 Builder scaffold

Created module shells + plan example + ops ADR paths per Architect manifest.
Stubs filled by Implementor in same crew pass (foreman in-process).
Optional dep: `pyproject.toml` `[gmi]` → `genblaze-gmicloud`.
