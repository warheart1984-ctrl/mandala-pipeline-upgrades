# 04 — Reviewer conformance

- No StoryForge imports in `app/*.py`.
- No key material in `/health` or response `byok` meta.
- Print SoT unchanged (`cpu.rt4d.print` / Digital Printer).
- Hosted default deny verified by `RENDER=true` unit test.
