# 03 — Implementor Notes

**Role:** Implementor · **Profile:** Integrator · **Mode:** Monk  
**Status:** skeleton

## Delivered

- Constitution v0.1 (**declared**), linked from README + RBC charter
- CLI: `new-cko`, `replay`, `test-reproducibility`
- Validator exits 0 with NOT FROZEN until archive hashes exist
- Minimal YAML loader fallback when PyYAML absent
- IME layers + near-round multiplication pattern + MATH CKOs

## Gaps (intentional)

- No media generation / TTS / upload
- Hash comparison after freeze not byte-comparing video
