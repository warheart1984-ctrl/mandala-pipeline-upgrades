# 02 — Builder Scaffold Manifest

**Trail:** `e2e-close-gaps-2026-07`  
**Status:** **enforced** (scaffolds landed with implementor)

| Artifact | Kind |
|----------|------|
| `src/node.js` | new Node export surface |
| Provenance hash + lineage receipt | extend existing runtime modules |
| CI job `runtime-provenance-replay` | workflow scaffold |
| CECP trail dir | this folder |
| Tests | extend gpu-core, ProvenanceRecorder, ReplayService, test_byok |
