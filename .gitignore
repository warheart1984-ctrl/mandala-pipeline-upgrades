# Axiom-X .gitignore

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
output/
4d-renderer/output/
# Optional/legacy local junction → mrs/packages/renderer-core/src (npm run link:mrs-shim)
4d-renderer/src/
# Leftover shim copy — canonical clients live under mrs/packages/renderer-core/clients
4d-renderer/clients/
examples/comparisons/out/
4d-renderer/node_modules/
cssv/node_modules/

# Unity
unity/**/Library/
unity/**/Temp/
unity/**/Obj/
unity/**/Logs/
unity/**/UserSettings/
unity/**/*.csproj
unity/**/*.sln
unity/**/*.userprefs
unity/**/MemoryCaptures/

# Unreal
unreal/**/Binaries/
unreal/**/DerivedDataCache/
unreal/**/Intermediate/
unreal/**/Saved/
unreal/**/.vs/
unreal/**/*.xcworkspace
unreal/**/*.opendb
unreal/**/*.opensdf
unreal/**/*.sdf
unreal/**/*.sln
unreal/**/*.suo
unreal/**/*.xcodeproj

# Env / secrets
.env
.venv

# IDE
.vscode/
.idea/
*.swp
.cursor/

# Upstream reference clone (pip packages remain the runtime dependency)
vendor/

# Python / local envs (hygiene)
.venv/
__pycache__/
.pytest_cache/
.mypy_cache/
*.pyc

# Local RT4D / WebGPU smoke artifacts (not product outputs)
mrs/packages/renderer-core/_tmp_*.png
mrs/packages/renderer-core/_debug_*.png
mrs/packages/renderer-web/_webgpu-smoke/_out/
*.wip-full
*.receipt-wip

# Temporary build artifacts
.tmp_*
.tmp-*/
tmp/
.worktrees/
renders/

# Runtime logs and probe artifacts (evidence/traces are regenerable)
logs/
*.err
test_pollinations.png
test_tts.mp3

# CDK context (local synth state)
infra/cdk/cdk.context.json

# HIP hello binary under proofs (JSON compile proof is SoT; rebuild with hipcc)
docs/4d-engine/proofs/legacy-efficient/hip_hello.exe
docs/4d-engine/proofs/legacy-efficient/*.exe
docs/4d-engine/proofs/legacy-efficient/hip-hello-*.tmp
docs/4d-engine/proofs/legacy-efficient/hip-hello-run.log

# Local package metadata / source references kept outside Git
jarvis-memoryboard/*.egg-info/
aiki/docs/reference-models/e8/sources/*.docx
VP_VULKANINFO_*.json

# Operator-provided production face GLBs (keep README / .gitkeep tracked)
operator-assets/**/*.glb
operator-assets/**/*.gltf
operator-assets/**/*.bin
!operator-assets/**/README.md
!operator-assets/**/.gitkeep

# Mandala Lattice v1.0 runtime — source of truth lives under sme/dist
# (dist/ is the deployed distribution layout for the desktop build)
!sme/dist/
!sme/dist/lattice/
!sme/dist/lattice/**
sme/dist/models/
sme/dist/node_modules/

# ARIS is a separate project (own git repo) — never absorb into MRS
Aris-/

# SME model weights and binaries — never tracked
models/
sme-suite/models/

# SME native build artifacts + vendored third-party (llama.cpp, whisper.cpp, onnxruntime)
sme-suite/build/
sme-suite/modules/*/build/
sme-suite/third_party/

# Parallel native working copy (canonical native source lives under sme-suite/modules)
modules/

# Mandala desktop app — binaries, weights, and secrets are never tracked
mandala-app/models/
mandala-app/config/api-keys.json
**/api-keys.json
