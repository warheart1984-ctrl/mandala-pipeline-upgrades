# Coverage Rules

1. Every governance module must have unit test coverage.
2. Critical security paths must have integration test coverage.
3. GPU encoder modules must have test coverage (mocked device).
4. Provenance, replay, and conformance modules must have test coverage.
5. Coverage regression must fail the CI build.
