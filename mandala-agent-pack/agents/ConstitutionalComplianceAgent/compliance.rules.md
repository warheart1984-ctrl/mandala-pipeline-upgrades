# Compliance Rules

1. CPU RT4D / CONTRACT_DIGITAL_PRINT is the sole authoritative print path.
2. GPU paths are assist-only — never elevate GPU to print authority.
3. Every evidence chain must be complete (no missing links).
4. All 5 constitutional principles (P1-P5) must be enforced.
5. All 7 runtime policies from default.policies.json must be satisfied.
6. All 16 conformance checks from default.conformance-profile.json must pass.
