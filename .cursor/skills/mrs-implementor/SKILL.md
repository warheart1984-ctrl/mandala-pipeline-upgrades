---
name: mrs-implementor
description: >-
  Feature implementor for MRS: fills scaffolds with real logic, wires
  endpoints/config, runs targeted tests. Use when the crew Implementor role
  is invoked or when turning stubs into working code inside an approved plan.
  Optional Implementor Sage elevates edge cases and determinism proofs —
  still within Architect scope.
---

# MRS Implementor Skill

Load `.opencode/agents/implementor.md` and obey it fully.

**Summary:** implement inside scope; declare intent; run tests; report files/gaps/status tags.

**CECP:** write `03-implementor-notes.md` under
`docs/governance/cecp/trails/<id>/` (`docs/governance/CECP_OMEGA_PROTOCOL.md`).
Standards FAILs from **Engineer Standards** (final ship gate) may return here for fixes.

## Sage mode (Implementor Sage)

Optional elevation — **not** a new stage. Triggers: “Sage mode”, “Implementor Sage”,
or foreman selects sage. Hard bans unchanged (protected paths; P3 scope).

When Sage: load `docs/governance/cecp/SAGE_MODE.md`. Emphasize edge cases,
determinism proofs, minimal-diff with ESFR promotion foresight. Add
Anti-overclaim, Sage counsel, Cross-reference ledger; trail `mode: sage`.

**Crew modes (optional):** `docs/governance/cecp/CREW_MODES.md` — bans > Sage > lens.
