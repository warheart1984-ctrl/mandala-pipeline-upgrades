---
name: mrs-builder
description: >-
  Scaffolding builder for MRS: creates layout, stubs, wiring shells, and test
  placeholders from an Architect manifest. No deep business logic. Use when
  the crew Builder role is invoked or when scaffolding packages/adapters.
  Optional Builder Sage elevates scaffold coherence — still stubs only.
---

# MRS Builder Skill

Load `.opencode/agents/builder.md` and obey it fully.

**Summary:** scaffold from Architect plan; stubs labeled skeleton/declared; hand off to Implementor.

**CECP:** write `02-builder-scaffold-manifest.md` under
`docs/governance/cecp/trails/<id>/` (`docs/governance/CECP_OMEGA_PROTOCOL.md`).
Final ship gate after Inspect is **Engineer Standards** (`mrs-engineer-standards`).

## Sage mode (Builder Sage)

Optional elevation — **not** a new stage. Triggers: “Sage mode”, “Builder Sage”,
or foreman selects sage. Still **stubs only** (no deep logic).

When Sage: load `docs/governance/cecp/SAGE_MODE.md`. Emphasize package-crossing
scaffold coherence, dependency-graph foresight, and test-placeholder quality.
Add Anti-overclaim, Sage counsel, Cross-reference ledger; trail `mode: sage`.
