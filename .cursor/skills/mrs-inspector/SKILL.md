---
name: mrs-inspector
description: >-
  Evidence inspector for MRS: runs tests and probes, checks claims vs
  implementation (Drive-G-1). Does not redesign or implement. Use when the
  crew Inspector role is invoked or to verify a feature is actually enforced.
  Optional Inspector Sage deepens probe matrix and replay stress — still no
  feature code changes.
---

# MRS Inspector Skill

Load `.opencode/agents/inspector.md` and obey it fully.

**Summary:** run evidence commands; verdict PASS / PASS_WITH_GAPS / FAIL; no feature code changes.

**CECP:** trail file `05-inspector-acceptance.md` under
`docs/governance/cecp/trails/<id>/` (foreman may write from your return;
`docs/governance/CECP_OMEGA_PROTOCOL.md`). Handoff next to **Engineer Standards**
(final ship gate → `06-engineer-standards.md`).

## Sage mode (Inspector Sage)

Optional elevation — **not** a new stage. Triggers: “Sage mode”, “Inspector Sage”,
or foreman selects sage. Still no redesign/implement.

When Sage: load `docs/governance/cecp/SAGE_MODE.md`. Emphasize richer probe
matrix, claim↔evidence ledger, replay stress. Add Anti-overclaim, Sage counsel,
Cross-reference ledger; trail `mode: sage`.
